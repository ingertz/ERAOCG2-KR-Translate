@CARDNAME_91310,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 티스티나의 변정
	CASE "略称"
		RESULTS = 티스티나 변정
	CASE "カテゴリ"
		RESULTS:0 = 티스티나
ENDSELECT


@CARD_91310(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 通常함정
	CASE "추가日"
		RETURN 251125
ENDSELECT

RETURN 0

@CARD_EXPLANATION_91310(종류)
#DIM DYNAMIC 종류

PRINTL このカード名の①②の효과は１ターンに１度、いずれかしか使用できない。
CALLFORM CARD_ABLE(1,마함起動,フリー誘発)
PRINTL ①：자신フィールドに「티스티나」モンスターが存在する場合に발동できる。상대の묘지のカード１枚を完全제외する。
PRINTL その「티스티나」モンスターがＥＸモンスターの場合、상대の묘지のカードを全て完全제외する事もできる。
CALLFORM CARD_ABLE(1,묘지起動,フリー誘発)
PRINTL ②：묘지のこのカードを제외して발동できる。자신の묘지の「티스티나」モンスター１体を손패に戻す。



@CARDCAN_91310(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,91310)
SIF RESULT == 1
	RETURN 0

IF 종류 == 마함起動 || 종류 == 묘지起動 || 종류 == フリー誘発
	SIF 발동済_1(듀얼리스트,91310)
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_제외NG置換") >= 1
		RETURN 0
	IF ゾーン == "마함"
		CALL CARD_SET_ACT(듀얼리스트,종류,ゾーン,場所)
		SIF RESULT != 1
			RETURN 0
		CALL FULLRESET
		효과範囲필드 = 1
		효과条件カテゴリ = 티스티나
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所,91310)
		SIF RESULT != 1
			RETURN 0
		CALL FULLRESET
		효과範囲상대묘지 = 1
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所,91310)
		SIF RESULT != 1
			RETURN 0
	ELSEIF ゾーン == "묘지"
		CALL FULLRESET
		효과範囲묘지 = 1
		효과条件カテゴリ = 티스티나
		효과条件종류 = モン全般
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所,91310)
		SIF RESULT != 1
			RETURN 0
	ELSE
		RETURN 0
	ENDIF
	RETURN 1
ENDIF


@CARDEFFECT_91310(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC 묘지送カード,5
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == 마함起動 || 종류 == 묘지起動 || 종류 == フリー誘発
	;カード발동確認
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,91310,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
		IF ゾーン == "마함"
			CALL FULLRESET
			효과範囲필드 = 1
			효과条件カテゴリ = 티스티나
			효과条件종류 = EXモン全般
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所,91310)
			IF RESULT == 1
				PRINTL 상대の묘지のカードを全て完全제외しますか？
				CALL YN_CHOICE(듀얼리스트)
			ELSE
				RESULT = 1
			ENDIF
			IF RESULT == 0
				FOR カウンタ,0,紙束最大値
					SIF GETVAR(@"%対面者%_묘지:カウンタ") == -1
						CONTINUE
					CALL CARD_DISPOSE(対面者,"묘지",カウンタ,完全제외行き)
				NEXT
			ELSE
				CALL FULLRESET
				효과条件行先플래그 = 完全제외行き
				CALL CARD_ADD_GY_BASIC(듀얼리스트, "상대묘지",ゾーン,場所)
			ENDIF
		ELSEIF ゾーン == "묘지"
			CALL FULLRESET
			효과条件カテゴリ = 티스티나
			효과条件종류 = モン全般
			CALL CARD_ADD_BASIC(듀얼리스트,"묘지",ゾーン,場所)
		ENDIF
	ENDIF
ENDIF
