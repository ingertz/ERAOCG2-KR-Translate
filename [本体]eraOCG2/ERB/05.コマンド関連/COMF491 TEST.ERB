;-------------------------------------------------
;TEST用
;-------------------------------------------------
@COM491
#DIM DYNAMIC 캐릭터_ID
#DIM DYNAMIC カードコスト,10
#DIM DYNAMIC 절정BASE計算
#DIM DYNAMIC 절정BASE基礎
#DIM DYNAMIC 이벤트主
#DIM DYNAMIC TOTAL

;CALL MAP_PRINT_RESORT

PRINTFORML CFLAG:MASTER:수영복착용:{CFLAG:MASTER:수영복착용}
PRINTFORML 
PRINTFORML MASTER:
FOR LOCAL,0,20
	PRINTFORML CFLAG:MASTER:(200 + {LOCAL}): {CFLAG:MASTER:(200 + LOCAL)}
NEXT
PRINTFORML 
PRINTFORML TARGET:
FOR LOCAL,0,20
	PRINTFORML CFLAG:TARGET:(200 + {LOCAL}): {CFLAG:TARGET:(200 + LOCAL)}
NEXT


;-------------------------------------------------
;実行판정
;-------------------------------------------------
@COM_ABLE491
;デバッグ中のみ표시
SIF デバッグシステム != 1
	RETURN 0
RETURN 1
